import { expect } from "chai"
import sinon from "sinon"

describe("Word watch functions", () => {
  describe("when I go to /", () => {
    it("shows me the top word and count on the right side of the page", () => {
      
    })
  })
})
